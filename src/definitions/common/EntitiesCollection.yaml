type: object
description: Collection of entities referenced in top-level object
properties:
  users:
    type: array
    items:
      $ref: '#/definitions/UserInfoBasic'
  timelines:
    type: array
    items:
      $ref: '#/definitions/Timeline'
