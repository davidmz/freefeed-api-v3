type: object
description: General type for all API errors.
required:
  - code
  - message
properties:
  code:
    type: string
    description: Machine-readable error code, for example `NotFound`
  message:
    type: string
    description: Human-readable error message
