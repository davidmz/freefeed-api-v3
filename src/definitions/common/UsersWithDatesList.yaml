type: array
description: List of user IDs with timestamps
items:
  type: object
  required:
    - id
    - date
  properties:
    id:
      type: string
      format: uuid
    date:
      type: string
      format: date-time
